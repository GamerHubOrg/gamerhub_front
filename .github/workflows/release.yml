name: "Create Release"
permissions:
  contents: write
on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'
jobs:
  build:
    name: Build
    runs-on: self-hosted
    steps:
    - name: Checkout code
      uses: actions/checkout@v2
    - uses: ncipollo/release-action@v1