interface IConfigTranslations {
  playModal: {
    playWith: string;
  };
  shareModal: {
    title: string;
    placeholder: string;
    toastSuccess: string;
    toastError: string;
  };
}

export default IConfigTranslations;
